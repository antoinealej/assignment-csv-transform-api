# Assignment csv transform api

## Install

```
yarn install
```

## Setup

Copy the `.env.default` file into `.env` and replace the content with your database details.

```
cp .env.defaults .env
```

## Run

Run the command `yarn start`

```
yarn start
```
